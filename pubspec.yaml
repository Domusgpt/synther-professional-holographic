name: synther_holographic_pro
description: "Professional 4D Holographic Synthesizer with Vaporwave Aesthetics"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.0
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # State management
  provider: ^6.1.2

  # Audio engines & synthesis
  permission_handler: ^11.4.0
  dart_melty_soundfont: ^2.0.0

  # UI & Animations
  flutter_animate: ^4.5.0
  glassmorphism: ^3.0.0
  flutter_shaders: ^0.1.2

  # WebView for 4D visualizer & vib34d-sdk integration
  webview_flutter: ^4.9.0
  webview_flutter_web: ^0.2.2+4
  webview_flutter_android: ^3.12.1
  webview_flutter_wkwebview: ^3.9.4

  # Sensors for quaternion-based XR
  sensors_plus: ^3.1.0

  # File handling
  file_picker: ^8.0.0+1

  # Math & utilities
  vector_math: ^2.1.4
  collection: ^1.18.0

  # Storage & Preferences
  shared_preferences: ^2.2.2
  path_provider: ^2.1.1

  # HTTP & API
  http: ^1.1.0

  # File I/O
  path: ^1.8.3

  # Firebase suite
  firebase_core: ^3.13.1
  firebase_auth: ^5.5.4
  cloud_firestore: ^5.6.8
  firebase_storage: ^12.4.6
  firebase_analytics: ^11.4.6
  firebase_messaging: ^15.2.6
  cloud_functions: ^5.5.1

  # Audio utilities
  wav: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^5.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true
  
  assets:
    - assets/visualizer/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
